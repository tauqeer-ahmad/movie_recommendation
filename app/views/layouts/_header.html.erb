<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "Movie Mania", root_path, :class=> 'brand' %>
      <div class="nav-collapse in collapse">
        <ul class="nav">
          <li class="dropdown">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">Help <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Users', "#" %></li>
              <li><%= link_to 'Movies', "#" %></li>
            </ul>
          </li>
        </ul>

        <div class="nav pull-right">
          <% unless user_signed_in? %>
            <div class="btn-group login-group">
              <%= link_to "Login", new_user_session_path, :class => 'btn btn-info' %>
              <%= link_to "Register", new_user_registration_path, :class => 'btn btn-info'%>
            </div>
          <% else %>
            <li class="dropdown">
              <a data-toggle="dropdown" class="dropdown-toggle" href="#"><%= current_user.display_name %><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to "Account", "#" %>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                </li>
              </ul>
            </li>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
